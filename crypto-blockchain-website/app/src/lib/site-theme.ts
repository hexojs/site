// Not under check-ui's scanned uiRoots on purpose: this is the one place
// the site's brand navy is allowed as a literal, for the <meta theme-color>
// tag that __root.tsx (a scanned route file) cannot inline directly.
export const SITE_THEME_COLOR = "#10142B";
